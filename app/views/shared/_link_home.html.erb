<div class="link">
	<div class="link-left">
		<div class="title_block">
		  <h3><span class="link_fav"><%= image_tag(link.favicon_url, :class=>"bordered-fav") if link.favicon_url %></span>
			    <span class="link_title"><%= link_to link.display_title, link.url %></span></h3>
		</div>
		<div class="description_block"><p><%= link.description %></p></div>
		<div class="meta_block">
			<h5 id="source"><div class="source-author-position"><%= link_to link.display_url, link.url %>
				<% if link.author %>
					<span class="pipe">|</span>
					<span class="author"><%= link.author%></span>
				<% end %></div></h5>
			<h5 id="time"><div class="time-position">
			<%= time_ago_in_words(link.created_at) + " ago"%></div></h5>
		</div>
	</div>
	<div class="link-right">
		<%= gravatar_image_tag(User.find(link.user_id).email, :alt => User.find(link.user_id).name, :class => 'mini-gravatar', :gravatar => { :size => 52 })%>
		<div class="shared-by"><p>Shared by </p> 
		<%= link_to User.find(link.user_id).name, user_path(User.find(link.user_id)) %></div>
		<div class="social_btn"><i class="icon-share"></i> Share</div>
	</div>
</div>